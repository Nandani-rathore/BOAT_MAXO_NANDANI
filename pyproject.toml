[project]
name = "bot-maxo"
version = "0.1.0"
description = "Gemini Chatbot"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "python-dotenv",
    "google-generativeai",
]
